<button type="button" class="collapsible"><div class="guild_button">
    <img src="{{this.avatar_url}}" alt="{{this.username}} ({{this.id}}) avatar" class="avatar" >
    </div>
    <div class="guild_button">
        <h4>{{this.username}}#{{this.discriminator}}</h3>
    </div>
</button>
    <div class="content">
        <button type="button" class="not_collapsible">ID: {{this.id}}</button>
        <button type="button" class="not_collapsible">Username: {{this.username}}</button>
        <button type="button" class="not_collapsible">Nickname: {{this.nickname}}</button>
        <button type="button" class="not_collapsible">Discriminator: {{this.discriminator}}</button>
    </div>